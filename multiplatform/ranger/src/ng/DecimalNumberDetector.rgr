Import "TokenDetector.rgr"

class DecimalNumberDetector {
    Extends(TokenDetector)

    Constructor (noMatchSlice:TokenSlice) {
        cachedNoMatch = noMatchSlice
        detectedTag = "decimal-number"
    }

    sfn create:DecimalNumberDetector () {
        def s (TokenDetector.createNoMatchSlice())
        return (new DecimalNumberDetector(s))
    }

    fn isDigit:boolean (ch:int) {
        if (ch < 48) {
            return false
        }
        if (ch > 57) {
            return false
        }
        return true
    }

    fn detect:TokenSlice (slice:TokenSlice) {
        def len (slice.length())
        if (len == 0) {
            return (this.noMatch())
        }

        def i@(mutable):int 0
        while (i < len) {
            def ch (slice.charCodeAt(i))
            if (this.isDigit(ch)) {
                i = i + 1
            } {
                break
            }
        }
        if (i == 0) {
            return (this.noMatch())
        }
        if (i >= len) {
            return (this.noMatch())
        }
        if ((slice.charCodeAt(i)) != 46) {
            return (this.noMatch())
        }

        def j@(mutable):int (i + 1)
        while (j < len) {
            def ch2 (slice.charCodeAt(j))
            if (this.isDigit(ch2)) {
                j = j + 1
            } {
                break
            }
        }

        if (j == (i + 1)) {
            return (this.noMatch())
        }

        def out (slice.read(j))
        out.tag = detectedTag
        return out
    }
}